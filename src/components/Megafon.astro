
To make the entire component smaller, you can adjust the width and height of the images directly in the class attribute. Here's how to update your code:

Updated Photo Component with Smaller Size:
html
Kopier kode
<div x-data="{ active: false, hover: false }" class="fixed bottom-5 left-5">
  <a href="#" @click.prevent="active = !active">
    <img
      @mouseover="hover = true"
      @mouseleave="hover = false"
      :src="hover ? (active ? '/svg/alternate-hover.svg' : '/svg/megafon-hover.svg') : (active ? '/svg/alternate.svg' : '/svg/megafon.svg')"
      :alt="hover ? 'Hovered Image' : 'Default Image'"
      :class="hover ? 'w-16 h-16'" 
      class="transition-all duration-300 ease-in-out"
    />
  </a>
</div>

<script src="https://cdn.jsdelivr.net/npm/alpinejs@3.12.0/dist/cdn.min.js" defer></script>